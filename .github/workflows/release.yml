name: Release packages

on:
  push:
    branches:
      - master
    paths:
      - '.changeset/**'
      - '.github/workflows/release.yml'
  workflow_dispatch:

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

permissions:
  actions: write
  contents: write
  id-token: write
  pull-requests: write

jobs:
  release:
    name: Create release PR or publish packages
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Set up pnpm
        uses: pnpm/action-setup@v6
        with:
          version: '>=11'

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22.14.0
          package-manager-cache: false

      - name: Set up npm trusted publishing
        run: npm install --global npm@11.19.0

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Create release PR or publish to npm
        id: changesets
        uses: changesets/action@v2
        with:
          commit-message: 'chore(release): 更新包版本'
          create-github-releases: true
          github-token: ${{ secrets.GITHUB_TOKEN }}
          pr-title: 'chore(release): 更新包版本'
          publish-script: pnpm release:publish
          version-script: pnpm release:version
        env:
          NPM_CONFIG_PROVENANCE: 'true'

      - name: Rebase and merge release PR
        if: steps.changesets.outputs['pr-number'] != ''
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_PR_NUMBER: ${{ steps.changesets.outputs['pr-number'] }}
        run: |
          gh pr merge "$RELEASE_PR_NUMBER" --repo "$GITHUB_REPOSITORY" --rebase
          gh workflow run release.yml --repo "$GITHUB_REPOSITORY" --ref master
