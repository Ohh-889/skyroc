# 正式环境。这份保留 Expo 的标准名字，因为 `expo export` / EAS Build 会把 NODE_ENV 设成
# production 并自动加载它 —— 云端构建没机会插 dotenv-cli，改名就等于打出没有接口地址的包。
# 本地想用正式配置起 dev server 就跑 `pnpm start:prod`（显式走 dotenv-cli）。
#
# `production` 同样不在 Metro 的 additionalExts 里，不会污染 dev server 的虚拟 env 模块。

APP_ENV=production

EXPO_PUBLIC_API_BASE_URL=https://m1.apifoxmock.com/m1/8747974-8535971-8438951/



# 需要按环境分微信应用 / Live Activity 推送开关时，在这里写同名 key 覆盖 .env：
# WECHAT_APP_ID= / WECHAT_UNIVERSAL_LINK= / LIVE_ACTIVITY_PUSH=
