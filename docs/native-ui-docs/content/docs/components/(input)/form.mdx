---
title: Form
description: 列表式布局的表单容器与字段
---

`Form` 把 `CellGroup` 的分组外观（分隔线 / 圆角 / 标题）与 `@skyroc/form` 的状态管理、校验上下文合成一层渲染，`FormItem` 则把标签、控件、错误与描述映射到 `Cell` 的各个插槽——因此整个表单读起来就是一张设置列表。需要自由布局时改用 [`FieldGroup` / `FieldItem`](/docs/components/field)。

```tsx
import { Form, FormComputedField, FormItem, useForm } from '@skyroc/native-ui';
```

## 基础用法

`useForm` 创建表单实例，`FormItem` 用 `name` 声明字段路径并把子组件接到表单上。`form.submit()` 触发校验，通过走 `onFinish`，失败走 `onFinishFailed`；`form.resetFields()` 重置回初始值。

<Demo src="@playground/form/FormBasic" />

字段内的输入控件通常配 `variant="none"`：行本身已经是 Cell，控件再画一层边框会显得重。

## 何时使用

- 设置页、信息录入页这类**列表式**表单：一行一个字段，标签在左、控件在右。
- 需要标签压在控件上方的自由布局时用 [`FieldGroup` / `FieldItem`](/docs/components/field)，两者的字段配置与校验体系完全一致。
- `Form` 只负责一组字段的外观，一屏里可以放多个 `Form` 共享同一个 `form` 实例，做成分组表单。

## 值适配

缺省的取值逻辑取回调的第一个参数，同时把 `TextInput` 的原生事件抹平成 `nativeEvent.text`，因此 `Input` 与走 `onChange(value)` 约定的控件都不需要额外配置。要接管这条链路时：

- `getValueFromEvent`：改「从回调参数里怎么取值」，拿到的是子组件回调的原始参数；
- `normalize`：值写入表单前的归一化，改的是存下来的值；
- `getValueProps`：值传给子组件前的转换，只改显示值；
- `trigger` / `valuePropName`：接入不走 `value` / `onChange` 约定的控件（如 `Switch` 的 `checked` / `onCheckedChange`）。

<Demo src="@playground/form/FormValueAdapter" />

弹层类控件（`Picker` / `DatePicker`）的值由 `onConfirm` 回传，把 `trigger="onConfirm"` 传给 `FormItem` 即可；弹层的显隐仍由页面自己持有，整行的 `onPress` 负责打开。

## 标签布局

`labelAlign` 决定标签与控件的关系：

| 取值    | 布局                     | 适用场景                       |
| ------- | ------------------------ | ------------------------------ |
| `left`  | 标签在左、控件在右（默认） | 短文本、开关、选择器           |
| `top`   | 标签压在控件上方          | 地址、备注这类长内容           |

`labelWidth` 只在 `labelAlign="left"` 时生效，默认 `120`。同一个表单里各行保持同一个 `labelWidth`，标签列才对得齐——`FormComputedField` 的默认值也是 `120`，就是为了和 `FormItem` 对齐。

`size` 提供三档密度，影响行高与标签 / 错误 / 描述的排版，同样**不会透传给子组件**。

| 尺寸 | 标签字号    | 提示字号    |
| ---- | ----------- | ----------- |
| `sm` | `text-sm`   | `text-2xs`  |
| `md` | `text-base` | `text-xs`   |
| `lg` | `text-lg`   | `text-sm`   |

<Demo src="@playground/form/FormLayout" />

`labelAlign="left"` 时错误与描述会被拆到 Cell 外面独占一行，并按「标签列宽 + Cell 内边距」缩进，与控件左对齐；`labelAlign="top"` 时提示跟着内容走，不需要缩进。

## 必填与校验

`rules` 与 `required` 共用一份事实：只写 `rules={[{ required: true }]}` 会自动显示星号，只写 `required` 会自动补一条 required 规则，显式写 `required={false}` 只隐藏星号、不动已有规则。校验失败时标签整体变红，并向子组件注入 `error`，`Input` 这类支持 error 变体的控件因此自动变红。

多条规则同时失败时提示位只显示第一条，改完再暴露下一条。

## 交互与禁用

`onPress` 让整行可点，通常用来打开选择器；箭头缺省由 `onPress` 推导，也可以用 `showArrow` / `arrowDirection` 显式控制。`disabled` 禁用整行交互，`trailing` 在控件与箭头之间插入自定义内容。

<Demo src="@playground/form/FormInteraction" />

## 样式覆盖

`Form` 的 `border` / `inset` / `title` / `classNames` 直接取自 `CellGroup`；`FormItem` 的 `className` 追加到字段根容器，`classNames` 按 slot 覆盖。

| 组件       | slot          | 作用位置                                        |
| ---------- | ------------- | ----------------------------------------------- |
| `Form`     | `root`        | CellGroup 根节点                                |
| `Form`     | `title`       | 分组标题                                        |
| `Form`     | `divider`     | 字段之间的分隔线                                |
| `FormItem` | `root`        | 字段根容器 View（背景色在这里）                 |
| `FormItem` | `cell`        | 内层 Cell 根节点，自身保持透明                  |
| `FormItem` | `control`     | 控件所在区域，右对齐 Switch / Rate 这类控件时改它 |
| `FormItem` | `label`       | 标签文字                                        |
| `FormItem` | `required`    | 必填星号                                        |
| `FormItem` | `extra`       | 错误与描述的外层容器                            |
| `FormItem` | `message`     | 错误文案                                        |
| `FormItem` | `description` | 描述文本                                        |

<Demo src="@playground/form/FormStyles" />

`labelRow`（星号与标签的排列容器）和 `extraRow`（左右布局下提示独占的那一行）是纯结构类，不开放覆盖。

## 计算字段

`FormComputedField` 依据 `deps` 声明的字段自动重算 `compute` 的结果，字段本身只读，布局与 `FormItem` 的左右布局完全一致。

<Demo src="@playground/form/FormComputed" />

子组件是 `Input` 时 `compute` 必须返回字符串——RN 的 `TextInput` 只接受字符串，返回数字不会显示。

## 稳定性说明

无论校验有没有出错，`FormItem` 的根元素结构都保持不变（始终是 `View > Cell`），提示区的间距只靠切换 className 实现。这一点是刻意的：根元素类型在 `Cell` / `View` 之间切换会让 React 卸载并重建整棵子树（含内部的 `TextInput`），表现为校验出错后键盘收起、焦点丢失或跳到别的输入框。

## API

### Form

除下表外，`Form` 还接受 `CellGroup` 的 `border` / `inset` / `title` / `classNames`。

<PropsTable
  data={[
    { name: 'form', description: 'useForm 创建的表单实例，不传时内部自建一个', type: 'FormInstance<Values>' },
    { name: 'initialValues', description: '表单初始值，只在首次挂载时生效', type: 'DeepPartial<Values>' },
    { name: 'title', description: '分组标题，string 自动包裹 Text', type: 'ReactNode' },
    { name: 'inset', description: '卡片式内嵌样式（左右留边、圆角）', type: 'boolean', default: 'false' },
    { name: 'border', description: '是否在字段之间插入分隔线', type: 'boolean', default: 'true' },
    {
      name: 'validateTrigger',
      description: '字段默认的校验触发时机，可被 FormItem 覆盖',
      type: 'string | string[]',
      default: "'onChange'"
    },
    { name: 'validateMessages', description: '默认错误文案模板，支持占位符', type: 'ValidateMessages' },
    {
      name: 'schema',
      description: '表单级 schema 校验（zod / valibot 等 Standard Schema 实现）',
      type: 'FormSchema<Values>'
    },
    { name: 'preserve', description: '字段卸载后是否保留值', type: 'boolean', default: 'true' },
    { name: 'clearOnDestroy', description: '表单卸载时是否清空数据', type: 'boolean', default: 'false' },
    { name: 'onFinish', description: '提交且校验通过的回调', type: '(values: Values) => void' },
    {
      name: 'onFinishFailed',
      description: '提交但校验失败的回调，参数含 errorFields / firstErrorName',
      type: '(errorInfo: ValidateErrorEntity<Values>) => void'
    },
    {
      name: 'onValuesChange',
      description: '任一字段值变化的回调',
      type: '(changedValues: Partial<Values>, values: Values) => void'
    },
    {
      name: 'onFieldsChange',
      description: '字段元信息（错误、校验状态等）变化的回调',
      type: '(changedFields: Meta[], allFields: Meta[]) => void'
    },
    { name: 'classNames', description: '分组各 slot 的类名覆盖，取自 CellGroup', type: 'SlotClassNames<CellGroupSlots>' },
    { name: 'ref', description: '根节点的 ref，用于 measure / 滚动定位等命令式操作', type: 'Ref<View>' }
  ]}
/>

### FormItem

<PropsTable
  data={[
    { name: 'name', description: '字段名，支持 a.b[0] 这样的路径', type: 'AllPathsKeys<Values>', required: true },
    {
      name: 'children',
      description: '子组件，接收注入的 value / onChange / error / disabled',
      type: 'ReactElement',
      required: true
    },
    { name: 'label', description: '标签文本，为空时标签列不渲染', type: 'string' },
    {
      name: 'labelAlign',
      description: '标签对齐方式，left 为左右排布，top 为标签压在控件上方',
      type: "'left' | 'top'",
      default: "'left'"
    },
    { name: 'labelWidth', description: '标签列宽度，仅 labelAlign 为 left 时生效', type: 'number', default: '120' },
    {
      name: 'required',
      description: '是否显示必填星号；缺省由 rules 推导，显式传 true 会自动补一条 required 规则',
      type: 'boolean'
    },
    { name: 'rules', description: '校验规则', type: 'Rule[]' },
    { name: 'description', description: '描述文本，显示在控件下方、错误文案之后', type: 'string' },
    {
      name: 'size',
      description: '尺寸，只影响行高与标签 / 错误 / 描述的排版，不会透传给子组件',
      type: "'sm' | 'md' | 'lg'",
      default: "'md'"
    },
    { name: 'onPress', description: '整行点击回调，通常用于打开选择器', type: '() => void' },
    { name: 'disabled', description: '禁用整行交互', type: 'boolean', default: 'false' },
    { name: 'showArrow', description: '是否显示右侧箭头，缺省由 onPress 推导', type: 'boolean' },
    {
      name: 'arrowDirection',
      description: '箭头方向，showArrow 为 false 时无效',
      type: "'down' | 'left' | 'right' | 'up'"
    },
    { name: 'trailing', description: '右侧自定义内容，位于控件与箭头之间', type: 'ReactNode' },
    { name: 'initialValue', description: '字段初始值，优先级低于 Form 的 initialValues', type: 'any' },
    { name: 'valuePropName', description: '值属性名', type: 'string', default: "'value'" },
    { name: 'trigger', description: '触发取值的回调名', type: 'string', default: "'onChange'" },
    {
      name: 'validateTrigger',
      description: '触发校验的回调名，传 false 则只在提交时校验；缺省跟随 Form',
      type: 'string | string[] | false'
    },
    {
      name: 'getValueFromEvent',
      description: '从子组件回调参数里取值，缺省取第一个参数并抹平 TextInput 的原生事件',
      type: '(...args: any[]) => any'
    },
    { name: 'getValueProps', description: '值传给子组件前的转换，只改显示值', type: '(value: any) => any' },
    {
      name: 'normalize',
      description: '值写入表单前的归一化，改的是存下来的值',
      type: '(value: any, prevValue: any, allValues: Values) => any'
    },
    { name: 'preserve', description: '子组件卸载后是否保留字段值', type: 'boolean', default: 'true' },
    { name: 'className', description: '字段根容器类名，合并到变体样式之后', type: 'string' },
    { name: 'classNames', description: '各 slot 的类名覆盖', type: 'FormItemClassNames' },
    { name: 'ref', description: '根节点的 ref，用于 measure / 滚动定位等命令式操作', type: 'Ref<View>' }
  ]}
/>

### FormComputedField

<PropsTable
  data={[
    { name: 'name', description: '字段名，计算结果写回表单的这个路径', type: 'AllPathsKeys<Values>', required: true },
    { name: 'deps', description: '依赖的字段名，任一变化都会触发重算', type: 'AllPathsKeys<Values>[]', required: true },
    {
      name: 'compute',
      description: '依据依赖字段计算当前值，get 按路径读取单个字段',
      type: '(get: (name: AllPathsKeys<Values>) => any, all: Values) => any',
      required: true
    },
    { name: 'children', description: '子组件，接收计算值并被置为只读', type: 'ReactElement', required: true },
    { name: 'label', description: '标签文本', type: 'string' },
    { name: 'labelWidth', description: '标签列宽度，与同表单的 FormItem 保持一致才能对齐', type: 'number', default: '120' },
    { name: 'required', description: '是否显示必填星号，计算字段不参与推导，只作展示', type: 'boolean', default: 'false' },
    { name: 'rules', description: '校验规则', type: 'Rule[]' },
    { name: 'description', description: '描述文本，显示在计算结果下方', type: 'string' },
    { name: 'size', description: '尺寸，只影响行高与标签 / 提示的排版', type: "'sm' | 'md' | 'lg'", default: "'md'" },
    { name: 'valuePropName', description: '值属性名', type: 'string', default: "'value'" },
    { name: 'preserve', description: '子组件卸载后是否保留字段值', type: 'boolean', default: 'true' },
    { name: 'className', description: '字段根容器类名', type: 'string' },
    { name: 'classNames', description: '各 slot 的类名覆盖，与 FormItem 同一套', type: 'FormItemClassNames' },
    { name: 'ref', description: '根节点的 ref', type: 'Ref<View>' }
  ]}
/>

### useForm

```ts
const [form] = useForm<Values>();
```

不传 `form` 时 `Form` 会自建一个实例，只有需要在组件里命令式读写表单时才必须自己创建。实例方法见 [FormInstance](#type-form-instance)。

## 类型

```ts
import type {
  AllPathsKeys,
  FormComputedFieldProps,
  FormInstance,
  FormItemClassNames,
  FormItemProps,
  FormItemSlots,
  FormProps,
  Meta,
  Rule,
  ValidateErrorEntity,
  ValidateMessages
} from '@skyroc/native-ui';
```

<UnionType
  name="FormItemSlots"
  description="FormItem / FormComputedField 可通过 classNames 覆盖的 slot 名称；纯结构的 labelRow / extraRow 不在其中。"
  type="'cell' | 'control' | 'description' | 'extra' | 'label' | 'message' | 'required' | 'root'"
/>

<UnionType
  name="FormItemClassNames"
  description="FormItem 的 classNames 取值形态，等价于 SlotClassNames<FormItemSlots>。"
  type="Partial<Record<FormItemSlots, string>>"
/>

<UnionType
  name="AllPathsKeys"
  description="表单值对象里所有可用的字段路径，形如 'user' | 'user.name' | 'list[0].id'。"
  type="string"
/>

<UnionType
  name="RuleType"
  description="内置类型校验器，Rule.type 缺省为 string。"
  type="'boolean' | 'date' | 'email' | 'enum' | 'float' | 'hex' | 'integer' | 'number' | 'regexp' | 'string' | 'url'"
/>

<UnionType
  name="FormSchema"
  description="表单级 schema：Standard Schema 实现（zod / valibot 等），或一个返回问题列表的异步函数。"
  type="StandardSchemaV1 | ((state: Values, name?: string | string[]) => Promise<StandardSchemaV1NormalizedIssue[]>)"
/>

<UnionType
  name="ValidateMessages"
  description="默认错误文案模板，按 required / string.minLength / number.max 这类分层键取值，值里可写 ${min} ${max} ${len} 占位符。"
  type="{ default?: string; required?: string; string?: { len?: string; maxLength?: string; minLength?: string; pattern?: string }; number?: { invalid?: string; len?: string; max?: string; min?: string }; date?: { invalid?: string; max?: string; min?: string }; email?: string; url?: string; ... }"
/>

<UnionType
  name="SlotClassNames"
  description="classNames 的取值形态：把 slot 名映射到类名，每个 slot 都可选。本页用到的 slot 见 FormItemSlots / CellGroupSlots。"
  type="Partial<Record<Slots, string>>"
/>

<TypeTable
  data={[
    {
      name: 'Rule',
      description: '单条校验规则；一条规则里可以同时写多个约束，按基础校验 → 类型校验 → 自定义校验的顺序执行。',
      fields: [
        { name: 'required', type: 'boolean', description: '是否必填（值为空即失败）。' },
        { name: 'type', type: 'RuleType', description: '启用哪个内置类型校验器，缺省为 string。' },
        { name: 'message', type: 'string', description: '覆盖默认错误文案。' },
        { name: 'pattern', type: 'RegExp', description: '字符串正则校验。' },
        { name: 'len', type: 'number', description: '精确的数值或字符串长度，取决于 type。' },
        { name: 'min', type: 'number | Date | string', description: '最小数值 / 最早日期。' },
        { name: 'max', type: 'number | Date | string', description: '最大数值 / 最晚日期。' },
        { name: 'minLength', type: 'number', description: '字符串最小长度。' },
        { name: 'maxLength', type: 'number', description: '字符串最大长度。' },
        { name: 'enum', type: 'any[]', description: 'type 为 enum 时的允许值，按深比较。' },
        { name: 'whitespace', type: 'boolean', description: '为 true 时纯空白字符串视为失败。' },
        { name: 'skipIfEmpty', type: 'boolean', description: '默认 true：值为空且非必填时跳过其余校验。' },
        { name: 'transform', type: '(value: any) => any', description: '校验前对原始值做转换。' },
        {
          name: 'validator',
          type: '(rule: Rule, value: any, values: any) => Promise<string | any> | string | undefined | null',
          description: '自定义校验，返回字符串表示失败；可异步。'
        },
        { name: 'debounceMs', type: 'number', description: '异步校验的防抖时长（毫秒）。' },
        { name: 'validateTrigger', type: 'string | string[]', description: '这条规则在哪些触发时机执行。' },
        { name: 'warningOnly', type: 'boolean', description: '为 true 时失败只算警告，不阻塞提交。' }
      ]
    },
    {
      name: 'FormInstance',
      description: 'useForm 返回的表单实例，本页只列常用方法。',
      fields: [
        { name: 'submit', type: '() => void', description: '校验全部字段并结算 onFinish / onFinishFailed。' },
        { name: 'resetFields', type: '(names?: AllPathsKeys<Values>[]) => void', description: '重置指定字段（缺省全部）到初始值。' },
        { name: 'getFieldValue', type: '(name: AllPathsKeys<Values>) => any', description: '读取单个字段的值。' },
        { name: 'getFieldsValue', type: '(names?: AllPathsKeys<Values>[]) => Values', description: '读取指定字段（缺省全部）的值。' },
        { name: 'setFieldValue', type: '(name: AllPathsKeys<Values>, value: any) => void', description: '写入单个字段的值。' },
        { name: 'setFieldsValue', type: '(values: DeepPartial<Values>) => void', description: '批量写入字段值。' },
        {
          name: 'validateFields',
          type: '(names?: AllPathsKeys<Values>[], opts?: { dirty?: boolean }) => Promise<boolean>',
          description: '手动校验指定字段（缺省全部）。'
        },
        { name: 'validateField', type: '(name: AllPathsKeys<Values>) => Promise<boolean>', description: '手动校验单个字段。' },
        { name: 'getFieldError', type: '(name: AllPathsKeys<Values>) => string[]', description: '读取单个字段的错误信息。' },
        { name: 'getField', type: '(name: AllPathsKeys<Values>) => Meta', description: '读取单个字段的完整元信息。' },
        { name: 'setDisabled', type: '(name: AllPathsKeys<Values>, disabled: boolean) => void', description: '设置字段禁用态，会注入给子组件。' },
        { name: 'setHidden', type: '(name: AllPathsKeys<Values>, hidden: boolean) => void', description: '设置字段隐藏态，隐藏后该字段不渲染。' },
        { name: 'arrayOp', type: '(name: AllPathsKeys<Values>) => { insert; move; remove; replace; swap }', description: '数组字段的增删改序操作。' }
      ]
    },
    {
      name: 'Meta',
      description: '单个字段的元信息，onFieldsChange 与 form.getField 都返回它。',
      fields: [
        { name: 'name', type: 'string', required: true, description: '字段路径。' },
        { name: 'value', type: 'any', required: true, description: '当前值。' },
        { name: 'errors', type: 'string[]', required: true, description: '错误信息列表。' },
        { name: 'warnings', type: 'string[]', required: true, description: '警告信息列表（warningOnly 规则产生）。' },
        { name: 'touched', type: 'boolean', required: true, description: '是否被用户交互过。' },
        { name: 'validated', type: 'boolean', required: true, description: '是否已完成过校验。' },
        { name: 'validating', type: 'boolean', required: true, description: '是否正在校验（异步规则）。' }
      ]
    },
    {
      name: 'ValidateErrorEntity',
      description: 'onFinishFailed 的入参。',
      fields: [
        { name: 'values', type: 'Values', required: true, description: '校验失败时的完整表单值。' },
        { name: 'errorCount', type: 'number', required: true, description: '出错字段的数量。' },
        { name: 'errorFields', type: 'Meta[]', required: true, description: '出错字段的元信息列表，可用于滚动定位。' },
        { name: 'errorMap', type: 'Record<string, string[]>', required: true, description: '字段路径 → 错误信息。' },
        { name: 'warningMap', type: 'Record<string, string[]>', required: true, description: '字段路径 → 警告信息。' },
        { name: 'firstErrorName', type: 'string', description: '第一个出错字段的路径，用于自动聚焦。' },
        { name: 'submittedAt', type: 'number', required: true, description: '校验失败的时间戳。' }
      ]
    }
  ]}
/>
