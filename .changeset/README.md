# Changesets

本目录保存公共 npm 包尚未发布的版本变更说明。

功能或修复涉及公共包时，在提交代码前执行 `pnpm changeset`，选择受影响的包、版本级别并填写面向使用者的变更说明。应用、文档、playground 和其他 `private: true` 的 workspace 不参与该流程。

完整发布规则见 [`packages/PUBLISHING.md`](../packages/PUBLISHING.md)。
